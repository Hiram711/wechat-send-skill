---
name: wechat-send
description: 往本机微信的某个会话（默认「文件传输助手」）发一条文本消息，全程只操作本机微信客户端，不经任何第三方服务。用户说「微信发给我」「发到文件传输助手」「给自己发条微信」，或者需要把一段内容/文件路径/结果送到手机上看时，用本 skill。
---

# 微信本地发送

把一段文本发到本机微信的指定会话。默认发给「文件传输助手」——那是用户自己的设备间通道，等于"发给自己"，在手机上立刻能看到。

**不出本机。** 不用企业微信机器人、不用服务号/PushPlus 之类的推送网关，只驱动本机微信客户端。代价是发送瞬间会抢一下前台焦点，一条约 40 秒。

## 怎么发

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/wx_send.ps1 -Target "文件传输助手" -Message "内容"
```

正文含换行、引号或大量中文时，写进 UTF-8 文件再传路径，别往命令行里拼：

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/wx_send.ps1 -Target "文件传输助手" -MessageFile "C:\path\body.txt"
```

| 参数 | 作用 |
| --- | --- |
| `-Target` | 必填。目标会话名，要和微信侧栏里显示的完全一致。 |
| `-Message` | 正文。多行用 `` `n `` 分隔。 |
| `-MessageFile` | 从 UTF-8 文件读正文，优先级高于 `-Message`。 |
| `-DryRun` | 走完整流程并把正文粘进输入框，但不按回车。第一次对一个新会话发之前建议先跑这个。 |
| `-KeepShots` | 保留过程截图。**截图含聊天内容，看完立刻删。** |
| `-ShotDir` | 截图目录，默认 `$env:TEMP\wx_send_shots`。 |

发之前先确认目标会话名在微信侧栏能搜到。脚本会 OCR 核对聊天标题，**不符就中止且一个字都不输入**——这道闸是防发错人的，别绕开。

## 退出码

| 码 | 含义 |
| --- | --- |
| 0 | 成功 |
| 1 | 其他错误 |
| 2 | 未找到微信窗口 |
| 3 | 无法前台化。用户正在用机器、和脚本抢焦点时会这样，属正常，隔一会儿重试即可 |
| 4 | 未定位到目标会话 |
| 5 | 标题核对失败，已中止（没敢往不确定的窗口里打字） |
| 6 | 回车后正文仍在输入框里，判定未发出 |
| 7 | 未定位到输入框，或三次粘贴都没能在框里确认正文（OCR 和像素判据都没过）。**注意 7 不清空输入框**，残留会留到下一轮，下一轮第 1 次必然因基线不干净被拒、第 2 次清空后才过 |
| 8 | 桌面不可交互（锁屏/息屏） |
| 9 | 前面的发送任务排队超时（300s），本次没发也没乱按 |

**3、6、8、9 都是"这次没发出去，但也没乱按"**，适合稍后重试。5 和 7 别自动重试，先看诊断。

## 锁屏时发不出去

屏幕锁了（或息屏）之后 Windows 不给任何程序操作桌面：`SetForegroundWindow` 失败、截屏全黑、合成的鼠标键盘落不到任何窗口上。也就是说**人离开电脑的时候，恰恰是这条路唯一做不到的时候**。

脚本开头会先探测桌面可用性，不可用就返回 8，不去做无用的输入动作。锁屏有两副面孔，两条都认：已息屏时前台窗口为 0 且截屏亮度约等于 0；锁屏界面刚亮起时前台是 `Windows.UI.Core.CoreWindow`、壁纸让亮度到 80 上下。

想要锁屏期间也能送达，只能接受消息出本机，那是另一个决定。调用方该做的是把消息存起来等解锁补发（`agent-task-msg` 就是这么做的）。

## 排查

先跑诊断，它打印 DPI 与屏幕尺寸、桌面是否锁屏、微信各顶层窗口（含类名、pid、哪个是 `MainWindowHandle`）、截屏亮度、OCR 引擎与识别语言。只读——不前台化、不点击、不写文件，所以它不会告诉你目标会话能不能定位到，那一步必须截图 + OCR，用 `-DryRun` 去验：

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File tools/wx_diag.ps1
```

两个更细的探针：

- `tools/screen_probe.ps1` —— DPI 感知下的真实屏幕尺寸、每个微信顶层窗口的矩形，以及是否超出屏幕。查「点击落空」先看这个。
- `tools/ocr_probe.ps1 -Image <png> -FromY <y> -ToY <y> -RightPanelOnly` —— 对一张截图跑 OCR，只打印指定区间的文本框。它用 AST 从 `wx_send.ps1` 里抽函数复用同一套 OCR 和几何实现，所以看到的就是发送时看到的。`-RightPanelOnly` 会挡掉左侧会话列表 —— **那里是用户的私人聊天，排查时别往输出里倒**。

`-KeepShots` 留下的截图含聊天内容，看完立刻删：

```bash
rm -rf "$TEMP/wx_send_shots"
```

改完 `.ps1` 先过一遍语法检查（PowerShell 5.1 要 CRLF + 单个 UTF-8 BOM，`tools/fix_ps1_encoding.py` 负责这个）：

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File tools/ps_syntax.ps1 scripts/wx_send.ps1
```

## 它是怎么工作的

微信 4.x（实测 4.1.13.65，`Weixin.exe`，Qt 5.15.14，窗口类 `Qt51514QWindowIcon`）是自绘界面，**没有 UIAutomation 树**，拿不到任何控件。所以只能：

前台化 → 整屏截图 → Windows 自带离线 OCR（不联网）认屏定位 → 合成鼠标键盘输入。

几何全部像素级自标定，不依赖固定分辨率、缩放比或主题。一次 OCR 约 10 秒，一条完整发送约 40 秒。

### 改代码前先看这几个坑

每一个都表现成「日志看着正常但结果不对」：

- **不要往微信窗口发 `{ESC}`** —— 4.x 会直接缩到托盘。
- **`PrintWindow` 对这个窗口返回全黑**，只能整屏截取再按窗口矩形裁。
- **一次 OCR 要 10 秒**，所以「截图」和「基于这张截图去点击」之间隔得很远。这期间前台可能被别的程序抢走，点击和 `Ctrl+V` 就全落到别人窗口上。所以点击/粘贴/回车前都要 `Assert-Fg` 重新夺回前台，并用 `Click-Live` 重读窗口原点（窗口可能被挪过）。DryRun 收尾的 `Ctrl+A`+`Delete` 尤其不能省这一步 —— 打在别人窗口上就是在别的程序里全选删除。
- **正文里的方括号**不能进 `-like`：`[C` 在通配符里是没闭合的字符类。一律用 `.Contains()`。
- **界面渲染有延迟**，点开会话后第一张截图里标题常常还没出来，所以标题核对要重试几次。重试只是反复「看」，不输入任何内容，不削弱「标题不符就中止」这道闸。
- **搜索结果选行用「聊天记录」当上界，不要拿分组标签当下界。** 搜索面板里同一个名字会出现多次：联系人一行、群聊若干行（渲染成「包含：名字」）、聊天记录里还有一行文本和联系人**完全一样**。点聊天记录那行进的是消息搜索视图不是会话，标题闸会报 exit 5。曾经的写法是取第一个匹配 `^(功能|联系人|聊天|通讯录|群聊)$` 的标签当下界 —— 实测给对方发过消息后，联系人头上的标签会从「联系人」变成「最常使用」，不在正则里，锚点就跳到下面的「群聊」，把真联系人排除掉，选中聊天记录那行。往正则里堆标签名补不完（微信改版 + OCR 读错），改成切在「聊天记录」上方：联系人和群聊都排在它上面。
- **左栏的会话行是开关不是按钮**：已经打开的会话再点一次会收起，所以定位到目标后要先判断它是不是已经是当前会话。
- **不要动 `SPI_SETFOREGROUNDLOCKTIMEOUT`。** 那是改全局系统设置，用户明确拒绝过。抢不到前台就返回 3，交给调用方重试。

### 怎么确认"粘上了""发出去了"

拿不到控件内容，只能截图 + OCR 比对。脚本从正文里挑一个短串当**探针**，看它出现在哪个区域。四个坑，都会表现成"明明成功却判失败"：

- **探针不能含数字和标点。** OCR 在这两类字符上很不可靠 —— 实测 `[补发 09-19 19:03]` 被读成 `[补发09．1919：03]`，连字符成了全角句点、冒号成了全角冒号。所以探针只取字母和汉字（`LText` 字段），要求精确相等的会话标题比对才用 `NText`。
- **探针必须取自单独一行。** OCR 按行返回，跨行拼出来的串不会出现在任何一行里。脚本挑第一行剥完还剩 4 个字以上的那行。
- **输入框会滚动。** 正文一长，粘完光标停在末尾，开头几行滚出可见区，而探针取的正是开头。粘完按一次 `^{HOME}` 把开头滚回来。
- **有些正文 OCR 一条结果都不返回。** 实测整行只由同一个汉字组成时（`哈`、`哈哈`、`哈哈哈`、`哈哈哈哈` 都试过），Windows OCR 干脆不把它当成一行文本；掺进任何一个别的字（`哈哈哈好`、`你哈哈哈`）立刻就能读。不是重复字的问题 —— `好好好` 正常。探针取自正文，于是这类消息**永远发不出去**：粘贴每次都成功，校验每次都失败，重试三次后 exit 7。这不是某个字的特例，任何 OCR 读不出的正文（单字、纯标点、纯表情）都会中招。

所以粘贴校验多了一条不依赖 OCR 的兜底：量输入框首行「有字的列数」，粘之前一次、粘之后一次，从"空"涨到"有字"就认。两处细节是实测踩出来的：

- **空输入框不是纯底色** —— WeChat 会在里面画一行淡色提示文字（实测 10 个字符、约 263 列）。按亮度分三档：底色 35、提示文字峰值 123、真正的正文峰值 224，判"有字"的偏离门槛取 130 才能把提示文字挡在外面（取 40 就会把空框数成有字，反而判成"粘完字变少了"）。130 门槛下空框 0 列、三个汉字 63 列。
- **数列数不数像素。** 空框里的文本光标实测 4px 宽、37px 高，按像素数它和一个汉字同量级，按列数只占 4 列。涨幅门槛跟着首行带高走（0.15 倍，下限 6）而不写死，因为字宽和带高都随 DPI 等比缩放。

**只有粘之前基线基本是空的（≤6 列）才认这条判据。** 像素法只能证明"框从空变成有字"，证明不了"框里的字是我们粘的"：框里原本那点字可能是用户自己的草稿，或者上一轮 exit 7 留下的残留（exit 7 不清框），认了就会把不该发的内容发出去。基线不干净就让下一轮先 `Ctrl+A`+`Delete` 清空重来 —— 第 2 次的基线就是干净的了。

"已发出"的判据是两条：输入框已清空（**这条才能认出失败** —— 粘贴步骤刚确认过正文在框里，回车生效的唯一表现就是它被清空），以及消息区出现探针。只看后者不行：消息区里的历史消息可能含同样的抬头，那等于问"有没有任何一条消息"，恰好把"回车没生效"报成成功。反过来，框空了但消息区没找到探针**不算失败** —— 消息区也会滚动，长正文发出后开头可能在可见区之上。

### 收尾会把现场放回去

两样东西，顺序是先窗口后指针：原来的前台窗口重新激活，鼠标指针回到发送前的位置（整条流程要点会话行和输入框，会把指针挪进微信窗口里）。

指针还原有个前提 —— **只有指针还停在脚本最后放的地方才还原**。中途用户回来自己动了鼠标就不动它，把他的指针抢回旧位置比不还原更烦人。比对用的是 `SetCursorPos` 之后 `GetCursorPos` 读回的实际落点，不是请求的坐标：多屏/缩放下请求值会被裁到屏幕范围内，拿请求值比对会把正常情况误判成"用户动过"。

### 并发

多个进程同时发会互相抢前台、把 `Ctrl+V` 打到对方的会话里。脚本用命名互斥体 `Global\WxSendSkillMutex` 串行化，拿不到就排队等，最长 300 秒，超时退 9（没发也没乱按，可稍后重试）。

`Global\` 前缀不能省：不加的话互斥只在当前登录会话内生效，跨会话等于没锁。

超时定 300 秒是因为**一次完整发送实测 60~70 秒**（标题核对、粘贴校验各要几轮 OCR，每轮十几秒）。超时值必须显著大于单次耗时，否则前面那个还在正常干活，后面这个就超时放弃了——锁在，却没排上队。300 秒约等于排 4~5 个的深度。

持有者进程被杀或崩溃时，等锁的一方会收到 `AbandonedMutexException`。**抛这个异常时锁其实已经归调用方了**，当成失败处理就会既不发送也不释放，白等一场，所以代码里把它认成"拿到锁"并记一行警告。

互斥体只保证"一次一个"，不保证先到先得（Windows 命名互斥体无 FIFO 保证）。要严格顺序得自己排队列文件，目前没这个需求。

## 安装

发送脚本可以直接跑，不需要安装任何东西。把 SKILL.md 注册进 `~/.claude/skills/` 才能让 Claude Code 自动加载它：

```bash
python tools/install_skill.py --dry-run
```

看清楚要写什么再去掉 `--dry-run`。它只做两件事：规范化所有 `.ps1` 的编码，把 SKILL.md 复制到 `~/.claude/skills/wechat-send/`（并在开头补一行本项目的绝对路径，这样换任何工作目录都能找到脚本）。**不碰 `settings.json`，不注册任何 hook。**

卸载：`python tools/install_skill.py --uninstall`。

