# 输入安全回归

在 Windows 上运行 `python -m unittest discover -s tests -v`。测试只提取函数，使用内存图像和模拟会话，不操作桌面。

2026-09-20 的真实联调中，896×648 窗口无法识别分栏时退回 `0.21 × 896 = 188`；真实分栏在 x=308。
随后计算的输入点击落入会话列表，切换了会话。发送校验失败，没有按回车，但留下测试草稿，用户已清理。

修正取消了猜测分栏，增加标题位置与输入框的交叉检查，并在点击后、清空前和最终发送前重新核对目标会话。
离线测试覆盖无分栏图像、明确分栏图像、误入列表的坐标和会话切换中止。
真实窗口 `-DryRun` 已通过：定位 x=308，点击 (384,529)，粘贴验证后清空，没有发送。
随后 Codex 审批、回合失败、等待回答三条真实通知均由本机成功发送，用户确认手机端三条全部收到（测试编号 `74b8a766`）。
